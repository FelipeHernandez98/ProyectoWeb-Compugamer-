<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                     <h3>Agregando tu opinion al Computador </h3><br>
                      <h4>{{pc.comp_marca}} {{pc.comp_ref}} </h4><br>
                      <h4>Publicado: {{timeago pc.created_at}}</h4>
                            <img src="{{pc.image_url}}" alt="{{comp_marca}}"
                             style="width:140px !important; height:140px !important">
                             <br>
                             <br>
                        <form action="/add_ref/{{pc.id}}" method="post">
                            
                            
                         <textarea name="reference" rows="5" cols="43">Escribe tu opinion sobre este equipo aqui ;)</textarea>
                         <button type="submit" class="btn btn-block btn-success">Enviar Referencia</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
<style type="text/css">
    body {
        font-family: 'Oswald', sans-serif !important;
        overflow: auto !important;
    }
</style>